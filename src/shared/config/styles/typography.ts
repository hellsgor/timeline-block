import { css } from 'styled-components';

export const typography = css`
  body {
    font-family: PT-sans, sans-serif;
    font-size: 20px;
    font-weight: 400;
    font-style: normal;
    line-height: 1.5;
  }
`;
